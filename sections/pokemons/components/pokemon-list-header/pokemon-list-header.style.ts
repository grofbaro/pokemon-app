import {StyleSheet} from "react-native";

export default StyleSheet.create({
    headerRow: {
        flexDirection: "row",
        paddingVertical: 12,
        paddingHorizontal: 12,
        marginRight: 102,
        marginBottom: 8,
    },
    headerText: {
        fontSize: 16,
        fontWeight: "600",
        color: "#333",
    },

});
